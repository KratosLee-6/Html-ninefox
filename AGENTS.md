# Repository agent instructions

## Agent skills

### Issue tracker

Issues and engineering work are tracked in GitHub Issues for `KratosLee-6/Html-ninefox`; pull requests are not part of the default triage queue. See `docs/agents/issue-tracker.md`.

### Triage labels

Use the default five-role vocabulary: `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, and `wontfix`. See `docs/agents/triage-labels.md`.

### Domain docs

This is a single-context repository. Read the root `CONTEXT.md` and relevant decisions under `docs/adr/` before changing domain behavior. See `docs/agents/domain.md`.
