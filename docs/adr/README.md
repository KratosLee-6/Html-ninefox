# Architecture decision records

This directory stores accepted architectural decisions for Html九尾狐.

Create an ADR only when a decision is difficult to reverse, has a real trade-off, and would appear surprising without its context. Use numbered filenames such as `0001-project-commit-journal.md`.

Domain vocabulary belongs in the repository root `CONTEXT.md`. Agent consumption rules are documented in `docs/agents/domain.md`.
